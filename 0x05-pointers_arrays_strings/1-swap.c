#include "main.h"
#include <stdio.h>
/**
 * swap_int - function to swap values of int
 * Description: 'swaping two int values'
 * @a: 'a holds the vareable address'
 * @b: 'value'
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int a;

	m = *a;
	*a = *b;
	*b = m;
}
